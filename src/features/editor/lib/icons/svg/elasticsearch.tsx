import type { SVGProps } from "react";

/**
 * Elasticsearch — stacked shards, narrowing. Monochrome; follows `currentColor`.
 */
export function ElasticsearchIcon(props: SVGProps<SVGSVGElement>) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={1.6}
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      {...props}
    >
      <path d="M4 5.6h16M6 10h14M8.6 14.4h11.4M12 18.8h8" />
    </svg>
  );
}
